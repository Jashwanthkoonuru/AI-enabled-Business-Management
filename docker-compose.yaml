version: "3.8"

services:
  reactjs:
    build: ./client
    container_name: react_app
    ports:
      - "80:80"
    restart: always

  java:
    build: ./backend_java
    container_name: java_app
    ports:
      - "8081:8080"
    restart: always
